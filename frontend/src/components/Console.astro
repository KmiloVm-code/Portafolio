<div
  class="console absolute top-1/2 right-0 -translate-y-1/2 p-4 hidden xl:block pointer-events-none z-10 text-xs overflow-hidden select-none w-[min(36vw,460px)] 2xl:w-[min(38vw,520px)]"
>
  <div class="rounded-2xl border border-border bg-bg-elevated shadow-xl backdrop-blur-md w-full">
    <div class="flex items-center gap-2 px-4 py-2 border-b border-border">
      <span class="h-2.5 w-2.5 rounded-full bg-red-400/90"></span>
      <span class="h-2.5 w-2.5 rounded-full bg-amber-300/90"></span>
      <span class="h-2.5 w-2.5 rounded-full bg-green-400/90"></span>
      <span class="ml-2 text-[11px] uppercase tracking-[0.16em] text-text-tertiary">build-log.ts</span>
    </div>

    <pre class="font-mono text-[11px] 2xl:text-xs p-5 text-text-secondary leading-relaxed w-full"><code>
<span class="text-accent">class</span> <span class="text-text-primary">Developer</span> <span class="text-accent">{`{`}</span>
  <span class="text-accent">constructor</span>(<span class="text-accent-hover">name</span>) <span class="text-accent">{`{`}</span>
    <span class="text-text-primary">this</span>.name = <span class="text-accent">"Andres Camilo"</span>;
    <span class="text-text-primary">this</span>.role = <span class="text-accent">"Full Stack Developer"</span>;
    <span class="text-text-primary">this</span>.stack = [
      <span class="text-accent">"React"</span>, <span class="text-accent">"Node.js"</span>, <span class="text-accent">"Cloud"</span>, <span class="text-accent">"AI"</span>
    ];
  <span class="text-accent">{`}`}</span>

  <span class="text-accent">async</span> createImpact() <span class="text-accent">{`{`}</span>
    <span class="text-accent">const</span> idea = <span class="text-accent">await</span> <span class="text-text-primary">this</span>.design();
    <span class="text-accent">return</span> <span class="text-text-primary">this</span>.ship(idea);
  <span class="text-accent">{`}`}</span>

  <span class="text-text-tertiary">// building clean products, line by line</span>
<span class="text-accent">{`}`}</span>

<span class="typing-line">
  <span class="text-accent">npm</span> <span class="text-text-primary">run</span> <span class="text-accent">build</span>
  <span class="cursor">|</span>
</span>
    </code></pre>
  </div>
</div>

<style>
  .cursor {
    animation: blink 1s step-end infinite;
  }

  .typing-line {
    display: inline-flex;
    align-items: center;
    gap: 0.1rem;
    animation: fadeTyping 1.6s ease both;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  @keyframes fadeTyping {
    0% { opacity: 0; transform: translateY(6px); }
    100% { opacity: 1; transform: translateY(0); }
  }
</style>
